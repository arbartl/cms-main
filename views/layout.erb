<!doctype html>
<html lang="en-US">
  <head>
    <title>CMS</title>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/cms.css" type="text/css">
  </head>
  <body>
    <header>
    <% if session[:message] %>
      <p class="message"><%= session.delete(:message) %></p>
    <% end %>
    </header>

    <main>
      <%= yield %>
    </main>
  </body>
</html>
